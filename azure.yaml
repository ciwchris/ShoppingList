# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: shopping-list
services:
  web:
    project: ./Client
    dist: bin/Release/net7.0/publish/wwwroot
    language: dotnet
    host: staticwebapp
  api:
    project: ./Api
    language: dotnet
    host: function
